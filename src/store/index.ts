export { useStore } from './useStore';
export { useStoreWithAuth } from './useStoreWithAuth';

// For backward compatibility and components that are already using useStore,
// we recommend migrating to useStoreWithAuth for authenticated operations